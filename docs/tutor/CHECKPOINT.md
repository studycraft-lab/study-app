# Live tutoring implementation checkpoint

Requested: epic #44, stories #45–#50. The user subsequently authorized merging the entire stack and delegated code-review decisions. All six PRs are now merged into main; normal Vercel deployment follows. Live activation remains pending configuration and real-session verification.
Baseline: main f5544a2. Read all seven issue bodies and comments (none), AGENTS.md, README, CI, local Next.js route/client guides and existing auth/import patterns.

## Status

- #45: contract, types, semantic validation, fixtures and external authoring guide implemented; lint/typecheck pass; full suite 155 passed, 1 optional CLI test skipped; build passes outside sandbox (sandbox TypeScript child output failed). 17 targeted contract tests pass. PR https://github.com/studycraft-lab/study-app/pull/51 (base main).
- #46: atomic import/publication, chapter mapping, parent library and vector/script preview complete. Full suite: 176 passed, 1 optional CLI test skipped; 6 tests execute SQL in PGlite. Lint/typecheck/final build pass; PR https://github.com/studycraft-lab/study-app/pull/52 (base #51). Shared animated rehearsal follows in #48.
- #48: generic animated player, shared parent rehearsal, bounded command state machine, child progress/CAS and stable resume implemented. Full suite 189 passed, 1 optional CLI test skipped; lint/typecheck/build pass. SQL tested locally; voice not tested. PR https://github.com/studycraft-lab/study-app/pull/53, stack base #52.
- #47: child section library/requests, parent catalogue/queue and published-pack fulfilment implemented. Full suite 197 passed, 1 optional CLI skipped, followed by 11 passing focused auth tests (5 new). Lint/typecheck/final build pass. PR https://github.com/studycraft-lab/study-app/pull/54; stack base #53.
- #49: WebRTC adapter, authenticated setup, portable trusted controller, bounded tools/board acknowledgements, SQL allowances and parent controls implemented. Lint/typecheck/build pass; full suite 242 passed, 1 optional CLI skipped, followed by two parent authorization tests. Provider/browser/controller tests are mocked; reservation tests execute local PGlite SQL. No real voice or multi-connection Supabase test. Merged PR https://github.com/studycraft-lab/study-app/pull/61. See VOICE.md for hosting requirements and unresolved hard-cap limits.
- #50: PDF recovered through the Drive link in issue #50 on 2026-09-15. Visually inspected printed p38 / PDF p5 and cytoplasm clarification p37 / PDF p4. Source-reviewed four-step pack and source review in `lesson-packs/icse-6-biology/plastids`; integrated local SQL rehearsal tests pass (2 tests). README/product/roadmap and `PILOT.md` handoff updated. Lint/typecheck/build pass. Full suite: 246 passed, 1 optional CLI skipped; additional four-step player UI test passes. Merged PR https://github.com/studycraft-lab/study-app/pull/62. POC ZIP and integrated real pilot still missing.

## Missing inputs / verification boundaries

User supplied `[insert paths]`. No supplied local POC ZIP or textbook PDF found initially. PDF now accessible from issue #50 via Google Drive; temporary download only, SHA-256 e25dea1fb949b0107b918a6d96c8d0f4b8d0ffb5902ffd8c7923b0cbea20e269. Requested POC ZIP path remains unanswered. Do not commit scans. `plastids-unverified.json` is solely a contract fixture based on issue outline. No voice or device testing performed. Feature must remain off until pilot acceptance. Existing exercise behavior must stay unchanged.

Required per-story gates: npm run lint; npm run typecheck; npm test; npm run build. Record results and PR URLs here. Parent auth is currently single-family (`ensureFamily`); child auth carries family/board/grade from server lookup. Supabase tables are service-role only with RLS. Use append-only migrations and explicit family filters.

## Merge result / resume

Merged in dependency order: #51 (#45) → #52 (#46) → #53 (#48) → #54 (#47) → #61 (#49) → #62 (#50). Every PR had passing CI and Vercel preview checks. Merge commits preserved branch ancestry. Main at `d429045` was verified byte-for-byte identical to the tested stack head `77f939e` before this documentation update. The local checkout is now main.

Historical state before 2026-09-19: hosted migrations/settings were pending. See the activation checkpoint below for the current deployed state. Set `TUTOR_ENABLED=true` for rehearsal; keep `TUTOR_LIVE_ENABLED=false` until controller hosting, credentials and real termination/session checks pass. Environment changes require a new Vercel deployment. See [activation steps](VOICE.md#activation-after-merge).

Next session: provision/verify the target database and server-only credentials; identify and configure one supervised controller host; obtain the POC ZIP for comparison; then execute `PILOT.md` on the intended tablet/browser and record actual provider termination/usage. #49/#50 acceptance and the epic remain incomplete despite the authorized code merges. Do not repeat completed authoring or substitute mock results for live acceptance.

## Activation access audit and controller packaging

The user asked Codex to drive activation end to end. GitHub merge/deployment access works, but its integration returns HTTP 403 for Actions secret inspection. No Vercel/Supabase/OpenAI credentials or local login configuration were present; no secret values were printed. Direct Vercel and Supabase integrations were discovered and suggested for connection. Account connection is pending; do not hand SQL/configuration work back to the user once connected.

A deployable controller image is now in `deploy/tutor-controller.Dockerfile`, with an allowlisted build context and a non-root runtime. AJV dependencies moved from development to production because the standalone controller imports validation code. Docker build and an isolated, read-only/network-disabled container smoke test passed: unauthenticated/wrong-token health requests return 401, authenticated unconfigured readiness returns 503, and SIGTERM exits 0. Lint/typecheck and 47 focused tests pass. These are local deployment checks, not real voice/provider evidence. No hosted service was provisioned or billed. The continuously running host remains to be selected/configured after account access and any necessary paid-host authorization.

## 2026-09-19 activation checkpoint

- Vercel and Supabase MCP OAuth connected. Supabase login required explicit supported scopes: organizations:read, projects:read, database:read, database:write. Vercel operations work using the connection's default scope; explicitly passing its returned team ID produces 403. No further sign-in is needed.
- Verified production Supabase project `llrgblwrywrauzfjledj` (studycraft). Existing exercise tables/data present; migration ledger was empty. Did not replay old migrations or change existing exercise rows.
- Applied all four tutoring SQL files in dependency order. MCP assigned ledger versions: `20260919034848` (20260915010000_create_tutor_content), `20260919034906` (20260915020000_create_tutor_progress), `20260919034919` (20260915030000_create_tutor_requests), `20260919034934` (20260915040000_create_tutor_voice_usage). These differ from repository filename versions: reconcile explicitly before any future CLI db push. Historical manual migrations still need schema-based reconciliation; automatic migration deployment remains pending, not enabled.
- Verified all six tutor tables have RLS enabled and no anon/authenticated SELECT access. Verified all nine tutor RPCs deny anon/authenticated execution and allow service_role execution.
- Validated Plastids v1 with the production validator and imported via the database import RPC into the existing ICSE Grade 6 Biology / The Cell chapter. Pack `c996c24a-4009-4f43-b782-2f965e3ef4f2`; hash `fb76c21188fa5368535edccd610403bb93fc558ef23cd1a87d7b27926625e7ec`. Kept draft: deployed authenticated visual rehearsal/publication has not occurred. This operator import does not establish browser-flow acceptance.
- Vercel project `prj_DpAp3SxFi2sHtqLICVoaSYUigADS`: set production TUTOR_ENABLED=true, TUTOR_LIVE_ENABLED=false, TUTOR_CONTROLLER_VERIFIED=false. Existing OpenRouter and Supabase credentials present; OPENAI_API_KEY/controller credentials absent. Did not decrypt any environment secrets.
- Redeployed tested main `7abcebfb1e1f93373bcacc0b92c9f102aaee0bc0`; deployment `dpl_3mFbLsdHNkkTgStMeV55zDEjRW6A` reached READY and aliases updated, including https://studycraft-iota.vercel.app. HTTP smoke: home and parent tutoring page 200; unauthenticated parent tutor API and child library API 401. No code changed, so full code suite was not rerun; prior CI applies to the same commit. These checks do not constitute authenticated exercise regression or real voice testing.
- User input requested: add OPENAI_API_KEY securely to Vercel Production, and identify existing continuous hosting or authorize selection of a priced option. No paid host provisioned. Host must use a single instance and stop-before-start replacements. Parent rehearsal/publication, controller credentials/hosting, migration automation credentials/history reconciliation, missing POC ZIP, and real device/provider acceptance remain outstanding.

### Controller hosting proposal

Verified Vercel Production OPENAI_API_KEY metadata is now present (value not decrypted; entitlement not tested). Prepared `deploy/render-tutor.yaml`: one paid Starter Docker web service in Singapore, automatic deploys off, 120-second graceful shutdown, voice initially off, secrets supplied at provisioning. A 1 GB disk enforces Render's documented single-instance, stop-before-start deployment behavior; it stores no lesson/audio data. Expected base hosting cost $7/month compute + $0.25/month disk, excluding taxes/overages/OpenAI usage, subject to checkout confirmation. No service purchased or provisioned; Render account access and cost approval remain required. Configuration reviewed against Render Blueprint docs; not validated by Render API or deployed. Existing Docker smoke evidence still applies to the unchanged image. User need not perform further Vercel configuration at this point.

### Hosting assumption challenged — reassess Vercel first

User questioned the extra server/cost and prefers existing Vercel hosting. Render proposal is NOT approved; do not provision it. Inspection confirms current controller uses process-local call state, an OpenAI sideband socket per call, deadlines and a five-second recovery loop. Those implementation choices require adaptation for bounded Vercel invocations; they do not establish that another provider is intrinsically required. Current official Vercel docs now document native WebSocket connections (https://vercel.com/kb/guide/do-vercel-serverless-functions-support-websocket-connections) and up to 30-minute Pro/Enterprise Function execution with extended-duration beta (https://vercel.com/changelog/vercel-functions-can-now-run-up-to-30-minutes). Earlier categorical hosting guidance was too strong. Next: verify actual account plan/runtime availability and design session ownership/reconnect/expiry recovery on Vercel before recommending external hosting. Do not remove server-authoritative progress or silently weaken usage limits. Original POC ZIP remains unavailable, so its implementation has not been compared.

### Child tutoring UI correction

User reported an unstyled entry above the study header, duplicated lessons, expanded request forms and inactive voice controls. Branch `fix/tutor-child-experience` moves entry inside the study library, presents one card per pack with saved progress taking precedence, collapses optional forms/help, groups the lesson into a responsive board/explanation layout, and removes call controls when unavailable. Voice remains disabled; this change does not establish real voice readiness. New mocked component regressions cover unavailable/available voice controls and one resume card per saved pack. Full suite 249 passed, 1 optional skipped; lint/typecheck/build passed. Chromium visual checks use mocked APIs (not real child login or voice): 1280px and 390px layouts, no horizontal overflow, no duplicate lesson link or inactive call buttons. Screenshot artifacts are temporary under /tmp/tutor-*.png. Actual screenshots inspected before merge. Pending work still includes Vercel voice adaptation and real provider/device tests.
